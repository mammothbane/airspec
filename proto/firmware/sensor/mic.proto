syntax = "proto3";

package airspecs.firmware.sensor;

import "nanopb.proto";

message Mic {
    message FFTFrame {
        uint32 bin = 1;
        uint32 value = 2;
    }

    repeated FFTFrame frames = 1 [(nanopb).max_count = 8];
    uint32 sample_rate_cHz = 2;
}
