syntax = "proto3";

package airspecs.firmware.sensor;

import "nanopb.proto";

message SGP {
    message SubSample {
        uint32 raw = 1 [(nanopb).int_size = IS_16];
        int32 index = 2;
    }

    SubSample voc = 1;
    SubSample nox = 2;
}

